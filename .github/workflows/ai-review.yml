# GGA AI Review Workflow
# Runs Gentleman Guardian Angel with OpenAI on PRs to main
# This flow reviews changes to the public organization repository.
# No secrets or internal information are exposed.

name: GGA AI Review

on:
  # Intentionally trusted: this workflow reads its definition from the base branch.
  # PR files are treated as data only; no PR scripts or actions are executed.
  pull_request_target:
    branches: [main]
    types: [opened, synchronize, reopened]

permissions:
  contents: read
  pull-requests: write
  checks: write

jobs:
  validate-workflow:
    name: Validate workflow syntax
    # Only run for PRs from the same repository (not external forks)
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - name: Checkout pull request content
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Run actionlint
        # Pinned to commit SHA of rhysd/actionlint@v1.7.7
        uses: rhysd/actionlint@03d0035246f3e81f36aed592ffb4bebf33a03106

  gga-review:
    name: GGA Review (OpenAI GPT-4.1 mini)
    runs-on: ubuntu-latest
    timeout-minutes: 10
    environment:
      name: gga-review
    needs: validate-workflow
    # Only run for PRs from the same repository (not external forks)
    if: github.event.pull_request.head.repo.full_name == github.repository

    steps:
      - name: Checkout repository
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          fetch-depth: 0
          ref: ${{ github.event.pull_request.head.sha }}

      - name: Fetch base branch for diff
        run: |
          git fetch origin "${{ github.event.pull_request.base.ref }}":"${{ github.event.pull_request.base.ref }}"

      - name: Verify AGENTS.md exists
        run: |
          if [[ ! -f "AGENTS.md" ]]; then
            echo "ERROR: AGENTS.md rules file not found in repository root"
            exit 1
          fi
          echo "AGENTS.md found ($(wc -l < AGENTS.md) lines)"

      - name: Run GGA review via OpenAI API
        id: gga
        env:
          OPENAI_API_KEY: ${{ secrets.OPENAI_API_KEY }}
        run: |
          set -Eeuo pipefail

          CHANGED_FILES=$(git diff --name-only "origin/${{ github.event.pull_request.base.ref }}...HEAD" || true)
          RESULT="reviewed"
          if [[ -z "$CHANGED_FILES" ]]; then
            RESULT="empty"
          fi
          echo "result=$RESULT" >> "$GITHUB_OUTPUT"

          if [[ "$RESULT" == "empty" ]]; then
            exit 0
          fi

          echo "Files to review:"
          echo "$CHANGED_FILES"

          REVIEW_FILE="/tmp/gga-review.md"
          true > "$REVIEW_FILE"

          while IFS= read -r file; do
            if [[ ! -f "$file" ]]; then
              continue
            fi

            echo "::group::Reviewing: $file"

            # Build JSON payload safely using jq
            PAYLOAD=$(jq -n \
              --arg model "gpt-4.1-mini" \
              --rawfile rules AGENTS.md \
              --rawfile content "$file" \
              --arg filename "$file" \
              '{
                model: $model,
                messages: [
                  {
                    role: "system",
                    content: "You are a code reviewer for ARKELYTHEX\u0027s organization. Analyze code against the provided coding standards. Be concise and specific. Output in markdown format."
                  },
                  {
                    role: "user",
                    content: ("Review the following file content against the coding standards below.\n\nRULES:\n" + $rules + "\n\nFILE: " + $filename + "\n\nCONTENT:\n" + $content + "\n\nPerform a code review following the rules above. Output in the specified format.")
                  }
                ],
                temperature: 0.1,
                max_tokens: 4096
              }')

            RESPONSE=$(curl -sS \
              -H "Content-Type: application/json" \
              -H "Authorization: Bearer $OPENAI_API_KEY" \
              -d "$PAYLOAD" \
              https://api.openai.com/v1/chat/completions)

            REVIEW=$(echo "$RESPONSE" | jq -r '.choices[0].message.content // "Error: No response from API"')
            {
              echo "## File: $file"
              echo ""
              echo "$REVIEW"
              echo ""
              echo "---"
              echo ""
            } >> "$REVIEW_FILE"

            echo "::endgroup::"
          done <<< "$CHANGED_FILES"

          echo "Review written to $REVIEW_FILE ($(wc -l < "$REVIEW_FILE") lines)"

      - name: Post review comment
        if: steps.gga.outputs.result == 'reviewed'
        uses: actions/github-script@60a0d83039c74a4aee543508d2ffcb1c3799cdea # v7.0.1
        with:
          script: |
            const fs = require('fs');
            const reviewPath = '/tmp/gga-review.md';
            if (!fs.existsSync(reviewPath)) {
              console.log('No review file found, skipping comment');
              return;
            }
            const review = fs.readFileSync(reviewPath, 'utf8').trim();
            if (!review) {
              console.log('Empty review, skipping comment');
              return;
            }
            const output = `## 🤖 GGA AI Review (OpenAI GPT-4.1 mini)\n\n${review}\n\n<!-- gga-review -->`;

            const { data: comments } = await github.rest.issues.listComments({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
            });

            const existing = comments.find(c =>
              c.body && c.body.includes('<!-- gga-review -->')
            );

            if (existing) {
              await github.rest.issues.updateComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                comment_id: existing.id,
                body: output,
              });
            } else {
              await github.rest.issues.createComment({
                owner: context.repo.owner,
                repo: context.repo.repo,
                issue_number: context.issue.number,
                body: output,
              });
            }
